#!/usr/bin/env python3

import csv
from pathlib import Path

import matplotlib.pyplot as plt
import numpy as np


DATA = Path("measurements_100k")
OUT = Path("results_board_3000")
OUT.mkdir(exist_ok=True)

TRACE_COUNT = 100000
BYTE_INDEX = 0


INV_SBOX = np.array([
    
], dtype=np.uint8)


def load_ciphertexts(path: Path) -> np.ndarray:
    rows = []

    with path.open() as f:
        for row in csv.reader(f):
            rows.append(
                np.frombuffer(
                    bytes.fromhex(row[0]),
                    dtype=np.uint8,
                )
            )

    return np.stack(rows)


def correlate(h: np.ndarray, traces: np.ndarray) -> np.ndarray:
    h = h.astype(np.float64)
    h -= h.mean()

    t = traces.astype(np.float64)
    t -= t.mean(axis=0)

    denominator = np.sqrt(
        np.sum(h * h) *
        np.sum(t * t, axis=0)
    )

    return np.divide(
        h @ t,
        denominator,
        out=np.zeros(traces.shape[1]),
        where=denominator != 0,
    )


def main() -> None:
    ciphertexts = load_ciphertexts(
        DATA / "collected_ciphertexts.csv"
    )[:TRACE_COUNT]

    traces = np.loadtxt(
        DATA / "collected_traces.csv",
        delimiter=",",
        dtype=np.float64,
    )[:TRACE_COUNT]

    print("ciphertexts:", ciphertexts.shape)
    print("traces:", traces.shape)

    for bit_index in range(8):
        correlations = np.zeros(
            (256, traces.shape[1]),
            dtype=np.float64,
        )

        c = ciphertexts[:, BYTE_INDEX]

        for guess in range(256):
            intermediate = INV_SBOX[
                np.bitwise_xor(c, np.uint8(guess))
            ]

            hypothesis = (
                (intermediate >> bit_index) & 1
            ).astype(np.float64)

            correlations[guess] = correlate(
                hypothesis,
                traces,
            )

        scores = np.max(
            np.abs(correlations),
            axis=1,
        )

        ranking = np.argsort(scores)[::-1]
        best = int(ranking[0])

        print(
            f"bit {bit_index}: "
            f"best=0x{best:02x}, "
            f"score={scores[best]:.6f}, "
            f"sample={np.argmax(np.abs(correlations[best]))}"
        )

        plt.figure(figsize=(11, 5))

        for candidate in ranking[1:10]:
            plt.plot(
                np.abs(correlations[candidate]),
                linewidth=0.7,
                alpha=0.35,
            )

        plt.plot(
            np.abs(correlations[best]),
            linewidth=2,
            label=f"best 0x{best:02x}",
        )

        plt.xlabel("Sample")
        plt.ylabel("|Pearson correlation|")
        plt.title(
            f"Board CPA — byte {BYTE_INDEX}, bit {bit_index}, "
            f"{len(traces)} traces"
        )
        plt.legend()
        plt.tight_layout()
        plt.savefig(
            OUT / f"byte{BYTE_INDEX}_bit{bit_index}.png",
            dpi=160,
        )
        plt.close()


if __name__ == "__main__":
    main()
